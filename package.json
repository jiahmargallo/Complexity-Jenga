{
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "vite": "^5.4.9"
  },
  "scripts": {
    "test": "npx vite",
    "build": "vite build",
    "predeploy" : "npm run build",
    "deploy" : "gh-pages -d build"
  }
}
